import React from 'react';
import PropTypes from 'prop-types';
import { Link } from 'react-router-dom';

function PreviousExercise(props) {
  return(
    <div>PreviousExercise</div>
  );
}

export default PreviousExercise;
